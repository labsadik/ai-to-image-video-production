# Solamentis

Provider-independent AI image generation platform.

The first implementation is intentionally logic-first: configuration, provider adapters, jobs, credits, safety, storage contracts, and database schema are separated from the UI.
