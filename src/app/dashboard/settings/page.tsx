'use client';

import { FormEvent, useEffect, useState } from 'react';
import { CheckCircle2, Loader2, Save, UserRound } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Card, CardBody, CardHeader } from '@/components/ui/card';
import { PageLoader } from '@/components/ui/page-loader';

const countries=[['IN','India'],['US','United States'],['BD','Bangladesh'],['GB','United Kingdom'],['AE','United Arab Emirates']];
type Profile={full_name:string|null;email:string|null;phone:string|null;country_code:string|null;country_name:string|null;locale:string|null;plan_id:string|null;created_at:string;updated_at:string;last_seen_at:string|null;last_login_at:string|null;login_count:number};
export default function SettingsPage(){const [profile,setProfile]=useState<Profile|null>(null);const [name,setName]=useState('');const [phone,setPhone]=useState('');const [country,setCountry]=useState('IN');const [message,setMessage]=useState('');const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);
 useEffect(()=>{fetch('/api/profile',{cache:'no-store'}).then(r=>r.json()).then(data=>{const p=data.profile as Profile;setProfile(p);setName(p?.full_name||'');setPhone(p?.phone||'');setCountry(p?.country_code||'IN');}).catch(()=>setMessage('Unable to load profile.')).finally(()=>setLoading(false));},[]);
 async function save(e:FormEvent){e.preventDefault();setSaving(true);setMessage('');try{const r=await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({full_name:name.trim(),phone:phone.trim(),country_code:country})});const data=await r.json();if(r.ok){setProfile(data.profile);setMessage('Profile saved successfully.');}else setMessage(data.error||'Unable to save profile.');}catch{setMessage('Network error. Please try again.');}finally{setSaving(false);}}
 if(loading)return <PageLoader label="Loading profile…"/>;
 return <div className="space-y-7"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-slate-400">Settings</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">Your profile</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Keep your identity, phone, country, and account activity connected to your Solamentis workspace.</p></div><div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_340px]"><Card><CardHeader><div className="flex items-center gap-3"><div className="grid size-11 place-items-center rounded-2xl bg-slate-100"><UserRound className="size-5"/></div><div><h2 className="text-sm font-semibold">Personal information</h2><p className="text-xs text-slate-400">Your email remains linked to your authenticated account.</p></div></div></CardHeader><CardBody><form onSubmit={save}><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium sm:col-span-2">Full name<input required autoComplete="name" value={name} onChange={e=>setName(e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100"/></label><label className="text-sm font-medium">Email<input disabled value={profile?.email||''} className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 bg-slate-50 px-3.5 py-3 text-slate-500"/></label><label className="text-sm font-medium">Phone number<input autoComplete="tel" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+91 …" className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100"/></label><label className="text-sm font-medium sm:col-span-2">Country<select value={country} onChange={e=>setCountry(e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100">{countries.map(([code,label])=><option key={code} value={code}>{label}</option>)}</select><span className="mt-2 block text-xs font-normal leading-5 text-slate-400">Used for regional pricing display and localization.</span></label></div>{message&&<p role="status" className={`mt-5 flex items-center gap-2 rounded-xl p-3 text-sm ${message.includes('success')?'bg-emerald-50 text-emerald-700':'bg-red-50 text-red-700'}`}>{message.includes('success')&&<CheckCircle2 className="size-4"/>}{message}</p>}<Button type="submit" loading={saving} className="mt-6">{saving?'Saving…':'Save profile'}{!saving&&<Save className="size-4"/>}</Button></form></CardBody></Card><Card><CardBody><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">Account record</p><div className="mt-6 space-y-5 text-sm"><div><p className="text-slate-400">Plan</p><p className="mt-1 font-semibold capitalize">{profile?.plan_id||'free'}</p></div><div><p className="text-slate-400">Member since</p><p className="mt-1 font-semibold">{profile?.created_at?new Date(profile.created_at).toLocaleDateString():'—'}</p></div><div><p className="text-slate-400">Last sign-in</p><p className="mt-1 font-semibold">{profile?.last_login_at?new Date(profile.last_login_at).toLocaleString():'Not recorded yet'}</p></div><div><p className="text-slate-400">Login activity</p><p className="mt-1 font-semibold">{profile?.login_count??0} sign-ins</p></div></div></CardBody></Card></div></div>;
}
