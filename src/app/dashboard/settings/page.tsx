'use client';

import { FormEvent, ReactNode, useEffect, useMemo, useState } from 'react';
import { CalendarDays, CheckCircle2, Clock3, Save, ShieldCheck, Sparkles, UserRound } from 'lucide-react';
import Link from 'next/link';
import { Button } from '@/components/ui/button';
import { Card, CardBody, CardHeader } from '@/components/ui/card';
import { PageLoader } from '@/components/ui/page-loader';
import { DISPLAY_PLANS, type PlanId } from '@/config/plan-display';
import { PlanComparison } from '@/components/plan-comparison';
import { BillingHistory, type BillingTransaction } from '@/components/billing-history';

const countries=[['IN','India'],['US','United States'],['BD','Bangladesh'],['GB','United Kingdom'],['AE','United Arab Emirates']];
type Profile={full_name:string|null;email:string|null;phone:string|null;country_code:string|null;country_name:string|null;locale:string|null;plan_id:PlanId|null;created_at:string;updated_at:string;last_seen_at:string|null;last_login_at:string|null;login_count:number};
type Subscription={id:string;plan_id:PlanId;status:string;provider:string|null;current_period_start:string|null;current_period_end:string|null;country_code:string|null;currency:string|null;cancel_at_period_end:boolean;created_at:string;updated_at:string};
type ApiPayload={profile:Profile;subscription:Subscription|null;planPrice:{currency:string;unit_amount_minor:number};planPrices:Record<string,number>;transactions:BillingTransaction[]};

export default function SettingsPage(){
 const [payload,setPayload]=useState<ApiPayload|null>(null); const [name,setName]=useState(''); const [phone,setPhone]=useState(''); const [country,setCountry]=useState('IN'); const [message,setMessage]=useState(''); const [loading,setLoading]=useState(true); const [saving,setSaving]=useState(false); const [compare,setCompare]=useState(false);
 useEffect(()=>{fetch('/api/profile',{cache:'no-store'}).then(async r=>{const data=await r.json();if(!r.ok)throw new Error(data.error||'Unable to load profile.');setPayload(data);setName(data.profile?.full_name||'');setPhone(data.profile?.phone||'');setCountry(data.profile?.country_code||'IN');}).catch(error=>setMessage(error instanceof Error?error.message:'Unable to load profile.')).finally(()=>setLoading(false));},[]);
 async function save(e:FormEvent){e.preventDefault();setSaving(true);setMessage('');try{const r=await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({full_name:name.trim(),phone:phone.trim(),country_code:country})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Unable to save profile.');setPayload(data);setMessage('Profile saved successfully.');}catch(error){setMessage(error instanceof Error?error.message:'Network error. Please try again.');}finally{setSaving(false);}}
 const plan=useMemo(()=>DISPLAY_PLANS.find(item=>item.id===(payload?.profile?.plan_id||'free'))||DISPLAY_PLANS[0],[payload]); const Icon=plan.icon; const currency=payload?.planPrice.currency||'USD'; const monthly=payload?.planPrice.unit_amount_minor??plan.monthlyFallbackMinor; const sub=payload?.subscription;
 if(loading)return <PageLoader label="Loading settings…"/>;
 return <div className="space-y-7 sm:space-y-8"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-slate-400">Settings</p><h1 className="mt-2 text-2xl font-semibold tracking-tight sm:text-4xl">Profile & plan</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Manage your profile, current plan, and verified payment history.</p></div>
   <section className="grid gap-5 xl:grid-cols-[minmax(0,1.1fr)_minmax(300px,.9fr)]"><Card><CardHeader><div className="flex items-center gap-3"><div className="grid size-11 place-items-center rounded-2xl bg-slate-100"><UserRound className="size-5"/></div><div><h2 className="text-sm font-semibold">Personal information</h2><p className="text-xs text-slate-400">Your email remains linked to your authenticated account.</p></div></div></CardHeader><CardBody><form onSubmit={save}><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium sm:col-span-2">Full name<input required autoComplete="name" value={name} onChange={e=>setName(e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100"/></label><label className="text-sm font-medium">Email<input disabled value={payload?.profile?.email||''} className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 bg-slate-50 px-3.5 py-3 text-slate-500"/></label><label className="text-sm font-medium">Phone number<input autoComplete="tel" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+91 …" className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100"/></label><label className="text-sm font-medium sm:col-span-2">Country<select value={country} onChange={e=>setCountry(e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100">{countries.map(([code,label])=><option key={code} value={code}>{label}</option>)}</select><span className="mt-2 block text-xs font-normal leading-5 text-slate-400">Used for regional pricing display and localization.</span></label></div>{message&&<p role="status" className={`mt-5 rounded-xl p-3 text-sm ${message.includes('success')?'bg-emerald-50 text-emerald-700':'bg-red-50 text-red-700'}`}>{message.includes('success')&&<CheckCircle2 className="mr-2 inline size-4"/>}{message}</p>}<Button type="submit" loading={saving} className="mt-6">{saving?'Saving…':'Save profile'}{!saving&&<Save className="size-4"/>}</Button></form></CardBody></Card>
     <Card><CardBody><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">Current plan</p><div className="mt-3 flex items-center gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-slate-950 text-white"><Icon className="size-5"/></span><div><h2 className="text-xl font-semibold capitalize">{plan.name}</h2><p className="text-xs text-slate-500">{plan.description}</p></div></div></div>{plan.id==='pro'&&<span className="rounded-full bg-slate-950 p-2 text-white shadow-lg" title="Premium"><Sparkles className="size-4"/></span>}</div><div className="mt-6 grid gap-3 sm:grid-cols-2"><Detail label="Started" value={sub?.created_at?new Date(sub.created_at).toLocaleDateString():payload?.profile?.created_at?new Date(payload.profile.created_at).toLocaleDateString():'—'} icon={<CalendarDays className="size-4"/>}/><Detail label="Current cost" value={monthly?`${currency} ${(monthly/100).toLocaleString()} / month`:'Free'} /><Detail label="Current period end" value={sub?.current_period_end?new Date(sub.current_period_end).toLocaleDateString():'No expiry on Free'} icon={<Clock3 className="size-4"/>}/><Detail label="Status" value={sub?.status||'active'} icon={<ShieldCheck className="size-4"/>}/></div><div className="mt-6 space-y-2">{plan.features.map(feature=><div key={feature} className="flex items-center gap-2 text-sm text-slate-700"><CheckCircle2 className="size-4 text-emerald-600"/>{feature}</div>)}</div><div className="mt-6 flex flex-col gap-2 sm:flex-row"><Link href="/dashboard/billing" className="inline-flex min-h-11 flex-1 items-center justify-center gap-2 rounded-xl bg-slate-950 px-4 text-sm font-semibold text-white hover:bg-slate-800">Manage plan</Link><button type="button" onClick={()=>setCompare(value=>!value)} className="inline-flex min-h-11 flex-1 items-center justify-center rounded-xl border border-slate-200 px-4 text-sm font-semibold text-slate-700 hover:bg-slate-50">{compare?'Hide comparison':'Compare all plans'}</button></div></CardBody></Card></section>
   <BillingHistory transactions={payload?.transactions??[]} />
   {compare&&<section className="space-y-4"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">Plan comparison</p><h2 className="mt-2 text-xl font-semibold">Compare features and period pricing</h2></div><PlanComparison prices={payload?.planPrices??{}} currency={currency} initialPlan={plan.id}/></section>}
 </div>;
}
function Detail({icon,label,value}:{icon?:ReactNode;label:string;value:string}){return <div className="min-w-0 rounded-2xl border border-slate-200 p-3.5"><div className="flex items-center gap-2 text-[11px] font-medium uppercase tracking-wide text-slate-400">{icon}{label}</div><p className="mt-1.5 break-words text-sm font-semibold text-slate-800">{value}</p></div>}
