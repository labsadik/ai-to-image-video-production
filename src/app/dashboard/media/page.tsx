'use client';

import type { ChangeEvent, FormEvent } from 'react';
import { useEffect, useMemo, useRef, useState } from 'react';
import { CheckCircle2, Clock3, Download, FileSearch, ImagePlus, Loader2, ScanSearch, Sparkles, Trash2, UploadCloud, Video, XCircle } from 'lucide-react';
import { PLATFORM_SPECS, type PlatformId } from '@/config/platforms';
import { ImageUpload, type UploadedReference } from '@/components/image-upload';

type Tab = 'image' | 'analysis' | 'video';
type Quality = 'preview' | 'standard' | 'premium';
type AnalysisLevel = 'basic' | 'medium' | 'hard';
type VideoQuality = 'standard';
type Job = { id: string; status: string; operation: string; prompt: string; size: string; quality: Quality; provider: string | null; model: string | null; output_path?: string | null; error_code: string | null; error_message: string | null; created_at: string; completed_at: string | null; project_id?: string | null; request: Record<string, unknown> };
type Output = { id?: string; variant: 'master' | 'preview'; storage_path: string; mime_type: string; width: number; height: number; byte_size: number; url: string | null };
type AnalysisResult = { classification: 'ai_generated' | 'edited_or_composited' | 'likely_real' | 'inconclusive'; ai_generated_probability: number; edited_probability: number; real_probability: number; inconclusive_probability: number; confidence: number; evidence: string[]; possible_editing_tools: string[]; limitations: string[] };

const platformLabels: Record<PlatformId, string> = { youtube_thumbnail: 'YouTube Thumbnail', instagram_post: 'Instagram Post', instagram_story: 'Instagram Story', facebook_post: 'Facebook Post', facebook_cover: 'Facebook Cover', pinterest_pin: 'Pinterest Pin', linkedin_post: 'LinkedIn Post', x_post: 'X Post', ad_creative: 'Ad Creative', poster: 'Poster', website_banner: 'Website Banner' };

function bytesLabel(bytes: number) { if (bytes >= 1024 * 1024) return `${(bytes / (1024 * 1024)).toFixed(2)} MB`; if (bytes >= 1024) return `${Math.round(bytes / 1024)} KB`; return `${bytes} B`; }
function percent(value: number) { return `${Math.round(Math.max(0, Math.min(1, value)) * 100)}%`; }
function asAnalysis(value: unknown): AnalysisResult | null {
  if (!value || typeof value !== 'object') return null;
  const item = value as Partial<AnalysisResult>;
  const classifications = ['ai_generated', 'edited_or_composited', 'likely_real', 'inconclusive'] as const;
  if (!classifications.includes(item.classification as typeof classifications[number])) return null;
  return {
    classification: item.classification as AnalysisResult['classification'],
    ai_generated_probability: typeof item.ai_generated_probability === 'number' ? item.ai_generated_probability : 0,
    edited_probability: typeof item.edited_probability === 'number' ? item.edited_probability : 0,
    real_probability: typeof item.real_probability === 'number' ? item.real_probability : 0,
    inconclusive_probability: typeof item.inconclusive_probability === 'number' ? item.inconclusive_probability : 0,
    confidence: typeof item.confidence === 'number' ? item.confidence : 0,
    evidence: Array.isArray(item.evidence) ? item.evidence.filter((x): x is string => typeof x === 'string') : [],
    possible_editing_tools: Array.isArray(item.possible_editing_tools) ? item.possible_editing_tools.filter((x): x is string => typeof x === 'string') : [],
    limitations: Array.isArray(item.limitations) ? item.limitations.filter((x): x is string => typeof x === 'string') : [],
  };
}
function statusTone(status: string) { return status === 'succeeded' ? 'bg-emerald-50 text-emerald-700' : status === 'failed' || status === 'cancelled' ? 'bg-red-50 text-red-700' : 'bg-amber-50 text-amber-700'; }

export default function MediaStudioPage() {
  const [tab, setTab] = useState<Tab>('image'); const [prompt, setPrompt] = useState(''); const [platform, setPlatform] = useState<PlatformId>('youtube_thumbnail'); const [quality, setQuality] = useState<Quality>('standard'); const [reference, setReference] = useState<UploadedReference | null>(null);
  const [analysisFile, setAnalysisFile] = useState<File | null>(null); const [analysisPreviewUrl, setAnalysisPreviewUrl] = useState<string | null>(null); const [analysis, setAnalysis] = useState<AnalysisResult | null>(null); const [analysisLevel, setAnalysisLevel] = useState<AnalysisLevel>('basic'); const [analysisSidebarOpen, setAnalysisSidebarOpen] = useState(true);
  const [duration, setDuration] = useState(5); const videoQuality: VideoQuality = 'standard'; const [aspectRatio, setAspectRatio] = useState('16:9');
  const [job, setJob] = useState<Job | null>(null); const [output, setOutput] = useState<Output | null>(null); const [previewOutput, setPreviewOutput] = useState<Output | null>(null); const [message, setMessage] = useState(''); const [loading, setLoading] = useState(false); const [loadingHistory, setLoadingHistory] = useState(false); const [historyId, setHistoryId] = useState<string | null>(null); const [downloading, setDownloading] = useState(false); const pollRef = useRef<ReturnType<typeof setTimeout> | null>(null);
  const spec = PLATFORM_SPECS[platform]; const active = job?.status === 'queued' || job?.status === 'processing'; const isVideo = job?.operation === 'generateVideoAd'; const persistedAnalysisPreview = previewOutput?.url ?? output?.url ?? analysisPreviewUrl;
  const storageComparison = useMemo(() => { if (!output) return null; if (!previewOutput) return { original: bytesLabel(output.byte_size), preview: null as string | null, ratio: null as string | null }; const ratio = output.byte_size > 0 ? (previewOutput.byte_size / output.byte_size) * 100 : 0; return { original: bytesLabel(output.byte_size), preview: bytesLabel(previewOutput.byte_size), ratio: `${ratio.toFixed(1)}% of original` }; }, [output, previewOutput]);

  async function loadJob(id: string, video = false) {
    try { const response = await fetch(`${video ? '/api/video-ad/' : '/api/generate/'}${id}`, { cache: 'no-store' }); const data = await response.json() as { job?: Job; output?: Output | null; preview?: Output | null; error?: string }; if (!response.ok || !data.job) throw new Error(data.error || 'Unable to read job status.'); setJob(data.job); setOutput(data.output ?? null); setPreviewOutput(data.preview ?? null); if (data.job.operation === 'analyzeImage') setAnalysis(asAnalysis(data.job.request?.analysisResult)); return data.job.status; }
    catch (error) { setMessage(error instanceof Error ? error.message : 'Unable to read job status.'); return null; }
  }
  async function loadHistory(id: string) {
    setLoadingHistory(true); setMessage('');
    try {
      const response = await fetch(`/api/history/${id}`, { cache: 'no-store' }); const data = await response.json() as { job?: Job; output?: Output | null; preview?: Output | null; error?: string }; if (!response.ok || !data.job) throw new Error(data.error || 'Unable to open saved media.');
      const saved = data.job.request ?? {}; const operation = data.job.operation; setTab(operation === 'analyzeImage' ? 'analysis' : operation === 'generateVideoAd' ? 'video' : 'image'); setHistoryId(id); setJob(data.job); setOutput(data.output ?? null); setPreviewOutput(data.preview ?? null); setPrompt(data.job.prompt); setAnalysis(asAnalysis(saved.analysisResult));
      if (data.preview?.url && operation === 'analyzeImage') setAnalysisPreviewUrl(data.preview.url); else if (data.output?.url && operation === 'analyzeImage') setAnalysisPreviewUrl(data.output.url);
      if (typeof saved.platform === 'string' && saved.platform in PLATFORM_SPECS) setPlatform(saved.platform as PlatformId);
      if (['preview', 'standard', 'premium'].includes(data.job.quality)) setQuality(data.job.quality as Quality);
      if (typeof saved.analysisLevel === 'string' && ['basic', 'medium', 'hard'].includes(saved.analysisLevel)) setAnalysisLevel(saved.analysisLevel as AnalysisLevel);
      if (typeof saved.durationSeconds === 'number') setDuration([5, 10].includes(saved.durationSeconds) ? saved.durationSeconds : 5);
      if (saved.videoQuality === 'standard') { /* production video uses one fixed quality */ }
      if (typeof saved.aspectRatio === 'string' && ['16:9', '9:16', '1:1'].includes(saved.aspectRatio)) setAspectRatio(saved.aspectRatio);
    } catch (error) { setMessage(error instanceof Error ? error.message : 'Unable to open saved media.'); }
    finally { setLoadingHistory(false); }
  }
  useEffect(() => { const id = new URLSearchParams(window.location.search).get('history'); if (id) void loadHistory(id); }, []);
  useEffect(() => () => { if (pollRef.current) clearTimeout(pollRef.current); if (reference?.previewUrl) URL.revokeObjectURL(reference.previewUrl); if (analysisPreviewUrl?.startsWith('blob:')) URL.revokeObjectURL(analysisPreviewUrl); }, [reference?.previewUrl, analysisPreviewUrl]);
  useEffect(() => { if (!job?.id || !active) return; const video = job.operation === 'generateVideoAd'; let cancelled = false; const poll = async () => { const status = await loadJob(job.id, video); if (!cancelled && status && ['queued', 'processing'].includes(status)) pollRef.current = setTimeout(poll, video ? 5000 : 1400); }; void poll(); return () => { cancelled = true; if (pollRef.current) clearTimeout(pollRef.current); }; }, [job?.id, job?.status]);

  async function submitImage(event: FormEvent) {
    event.preventDefault(); setLoading(true); setMessage(''); setJob(null); setOutput(null); setPreviewOutput(null); setHistoryId(null); setAnalysis(null);
    try { const response = await fetch('/api/generate', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Idempotency-Key': crypto.randomUUID() }, body: JSON.stringify({ operation: 'generateImage', prompt: prompt.trim(), platform, quality, size: `${spec.width}x${spec.height}`, width: spec.width, height: spec.height, referenceImageStoragePaths: reference?.path ? [reference.path] : [] }) }); const data = await response.json() as { jobId?: string; error?: string }; if (!response.ok || !data.jobId) throw new Error(data.error || 'Image generation could not be started.'); setJob({ id: data.jobId, status: 'queued', operation: 'generateImage', prompt: prompt.trim(), size: `${spec.width}x${spec.height}`, quality, provider: null, model: null, error_code: null, error_message: null, created_at: new Date().toISOString(), completed_at: null, request: { platform } }); await loadJob(data.jobId); setMessage('Image generation queued.'); }
    catch (error) { setMessage(error instanceof Error ? error.message : 'Image generation failed.'); } finally { setLoading(false); }
  }
  async function submitAnalysis() {
    if (!analysisFile) { setMessage('Choose an image to analyze. Analysis is intentionally separate from the generation upload safety gate.'); return; }
    setLoading(true); setMessage(''); setAnalysis(null); setJob(null); setOutput(null); setPreviewOutput(null); setHistoryId(null);
    try {
      const form = new FormData(); form.append('image', analysisFile); form.append('level', analysisLevel); const response = await fetch('/api/analyze-image', { method: 'POST', body: form }); const data = await response.json() as { result?: unknown; jobId?: string; output?: Output; preview?: Output; error?: string }; if (!response.ok || !data.result) throw new Error(data.error || 'Image analysis failed.'); const parsed = asAnalysis(data.result); if (!parsed) throw new Error('The analysis response could not be normalized.');
      setAnalysis(parsed); setOutput(data.output ?? null); setPreviewOutput(data.preview ?? null); setAnalysisPreviewUrl(data.preview?.url ?? data.output?.url ?? null);
      if (data.jobId) { setHistoryId(data.jobId); const request: Record<string, unknown> = { analysisResult: parsed, analysisLevel, originalByteSize: data.output?.byte_size, previewByteSize: data.preview?.byte_size }; setJob({ id: data.jobId, status: 'succeeded', operation: 'analyzeImage', prompt: `Image authenticity analysis · ${analysisLevel}`, size: data.output ? `${data.output.width}x${data.output.height}` : '', quality: 'preview', provider: 'google', model: null, error_code: null, error_message: null, created_at: new Date().toISOString(), completed_at: new Date().toISOString(), request }); }
      setMessage(`Analysis saved to history${data.jobId ? ` · ${data.jobId}` : ''}.`);
    } catch (error) { setMessage(error instanceof Error ? error.message : 'Image analysis failed.'); } finally { setLoading(false); }
  }
  async function submitVideo() {
    if (prompt.trim().length < 3) { setMessage('Describe the product, scene, movement, camera, and constraints.'); return; }
    setLoading(true); setMessage(''); setJob(null); setOutput(null); setPreviewOutput(null); setHistoryId(null); setAnalysis(null);
    try { const response = await fetch('/api/video-ad', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Idempotency-Key': crypto.randomUUID() }, body: JSON.stringify({ prompt: prompt.trim(), durationSeconds: duration, quality: videoQuality, aspectRatio }) }); const data = await response.json() as { jobId?: string; error?: string; model?: string; provider?: string }; if (!response.ok || !data.jobId) throw new Error(data.error || 'Video generation could not be started.'); setJob({ id: data.jobId, status: 'queued', operation: 'generateVideoAd', prompt: prompt.trim(), size: aspectRatio, quality: 'standard', provider: data.provider ?? null, model: data.model ?? null, error_code: null, error_message: null, created_at: new Date().toISOString(), completed_at: null, request: { durationSeconds: duration, videoQuality, aspectRatio } }); await loadJob(data.jobId, true); setMessage(`Video queued · ${data.provider ?? 'provider'} · ${data.model ?? ''}`); }
    catch (error) { setMessage(error instanceof Error ? error.message : 'Video generation failed.'); } finally { setLoading(false); }
  }
  async function downloadCurrent() {
    if (!historyId) return; setDownloading(true); try { const response = await fetch(`/api/history/${historyId}/download`, { cache: 'no-store' }); if (!response.ok) { const data = await response.json().catch(() => ({})) as { error?: string }; throw new Error(data.error || 'Download failed.'); } const blob = await response.blob(); const url = URL.createObjectURL(blob); const anchor = document.createElement('a'); anchor.href = url; anchor.download = `solamentis-${historyId}${isVideo ? '.mp4' : output?.mime_type === 'image/jpeg' ? '.jpg' : output?.mime_type === 'image/png' ? '.png' : '.webp'}`; document.body.appendChild(anchor); anchor.click(); anchor.remove(); URL.revokeObjectURL(url); } catch (error) { setMessage(error instanceof Error ? error.message : 'Download failed.'); } finally { setDownloading(false); }
  }
  async function deleteCurrent() { if (!historyId || !window.confirm('Delete this history item and its stored assets?')) return; const response = await fetch(`/api/history/${historyId}`, { method: 'DELETE' }); const data = await response.json() as { error?: string }; if (!response.ok) { setMessage(data.error || 'Unable to delete.'); return; } window.location.href = '/dashboard/history'; }
  function chooseTab(next: Tab) { setTab(next); setMessage(''); setJob(null); setOutput(null); setPreviewOutput(null); setAnalysis(null); setHistoryId(null); if (next !== 'analysis') { setAnalysisFile(null); if (analysisPreviewUrl?.startsWith('blob:')) URL.revokeObjectURL(analysisPreviewUrl); setAnalysisPreviewUrl(null); } }
  function onAnalysisFile(event: ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0] ?? null; setAnalysisFile(file); setAnalysis(null); setOutput(null); setPreviewOutput(null); setHistoryId(null); setMessage(''); if (analysisPreviewUrl?.startsWith('blob:')) URL.revokeObjectURL(analysisPreviewUrl); setAnalysisPreviewUrl(file ? URL.createObjectURL(file) : null); }

  return <div className="space-y-7 sm:space-y-8">
    <header><p className="text-xs font-semibold uppercase tracking-[.18em] text-slate-400">AI Media Studio</p><h1 className="mt-2 text-2xl font-semibold tracking-tight sm:text-3xl">Create, analyze, and review everything in one place.</h1><p className="mt-2 max-w-4xl text-sm leading-6 text-slate-500">Image generation, image authenticity analysis, silent video generation, saved masters, private storage, credits, and unified history.</p></header>
    <nav className="grid grid-cols-3 gap-2 rounded-2xl border border-slate-200 bg-white p-2 shadow-sm">{([['image', ImagePlus, 'Image generation'], ['analysis', ScanSearch, 'Image analysis'], ['video', Video, 'Video generation']] as const).map(([id, Icon, label]) => <button key={id} type="button" onClick={() => chooseTab(id)} className={`min-h-12 rounded-xl px-3 text-xs font-semibold transition sm:text-sm ${tab === id ? 'bg-slate-950 text-white' : 'text-slate-500 hover:bg-slate-50 hover:text-slate-900'}`}><Icon className="mr-2 inline-block size-4"/>{label}</button>)}</nav>
    {tab === 'image' && <form onSubmit={submitImage} className="grid gap-6 lg:grid-cols-[1.05fr_.95fr]"><section className="space-y-5 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div><h2 className="text-lg font-semibold">Generate an image</h2><p className="mt-1 text-sm text-slate-500">Select a quality, platform, and prompt. Image generation is routed by the production registry.</p></div><label className="block text-sm font-medium">Platform<select value={platform} onChange={event => setPlatform(event.target.value as PlatformId)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm">{Object.entries(platformLabels).map(([id, label]) => <option key={id} value={id}>{label} · {PLATFORM_SPECS[id as PlatformId].width}×{PLATFORM_SPECS[id as PlatformId].height}</option>)}</select></label><div className="grid grid-cols-3 gap-2">{(['preview','standard','premium'] as Quality[]).map(value => <button key={value} type="button" onClick={() => setQuality(value)} className={`rounded-xl border px-3 py-3 text-xs font-semibold capitalize ${quality === value ? 'border-slate-950 bg-slate-950 text-white' : 'border-slate-200 text-slate-600'}`}>{value}</button>)}</div><label className="block text-sm font-medium">Prompt<textarea value={prompt} onChange={event => setPrompt(event.target.value)} rows={7} required minLength={3} placeholder="Describe the subject, composition, lighting, style, text placement, and constraints…" className="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3 text-sm"/></label><ImageUpload current={reference} onChange={setReference}/><button type="submit" disabled={loading} className="inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-xl bg-slate-950 px-4 text-sm font-semibold text-white disabled:opacity-50">{loading ? <Loader2 className="size-4 animate-spin"/> : <Sparkles className="size-4"/>}Generate image</button></section><ResultPanel job={job} output={output} previewOutput={previewOutput} message={message} loadingHistory={loadingHistory} onDownload={downloadCurrent} onDelete={deleteCurrent} downloading={downloading} isVideo={false}/></form>}
    {tab === 'analysis' && <div className={`grid gap-6 ${analysisSidebarOpen ? 'lg:grid-cols-[.9fr_1.1fr]' : 'lg:grid-cols-[.42fr_1fr]'}`}>
      <section className="space-y-5 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div><h2 className="text-lg font-semibold">Image authenticity analysis</h2><p className="mt-1 text-sm leading-6 text-slate-500">Inspect an image and receive AI-generated, edited, real, and inconclusive percentages. Analysis is separate from image-generation safety checks.</p></div>
        <label className="flex min-h-48 cursor-pointer flex-col items-center justify-center rounded-2xl border-2 border-dashed border-slate-300 bg-slate-50 p-5 text-center hover:border-slate-400"><input type="file" accept="image/jpeg,image/png,image/webp,image/gif,image/bmp,image/tiff" hidden onChange={onAnalysisFile}/><UploadCloud className="size-7 text-slate-500"/><span className="mt-3 text-sm font-semibold text-slate-800">Choose image to analyze</span><span className="mt-1 text-xs text-slate-500">JPG, PNG, WebP, GIF, BMP, TIFF · max 25 MB</span></label>
        {analysisFile && <div className="rounded-2xl border border-slate-200 bg-slate-50 p-3"><div className="flex gap-4"><div className="size-24 overflow-hidden rounded-xl bg-slate-950"><img src={analysisPreviewUrl ?? undefined} alt="Uploaded image preview" className="h-full w-full object-contain"/></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-slate-900">{analysisFile.name}</p><p className="mt-1 text-xs text-slate-500">Original upload · {bytesLabel(analysisFile.size)}</p><p className="mt-1 text-xs text-slate-400">SHA-256 is recorded with the history item.</p></div></div></div>}
        <div className="grid grid-cols-3 gap-2">{(['basic','medium','hard'] as AnalysisLevel[]).map(value => <button key={value} type="button" onClick={() => setAnalysisLevel(value)} className={`rounded-xl border px-3 py-3 text-xs font-semibold capitalize ${analysisLevel === value ? 'border-slate-950 bg-slate-950 text-white' : 'border-slate-200 text-slate-600'}`}>{value}</button>)}</div>
        <button type="button" disabled={loading || !analysisFile} onClick={() => void submitAnalysis()} className="inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-xl bg-slate-950 px-4 text-sm font-semibold text-white disabled:opacity-50">{loading ? <Loader2 className="size-4 animate-spin"/> : <ScanSearch className="size-4"/>}Analyze image</button>{message && <p className="text-sm font-medium text-slate-600">{message}</p>}
      </section>
      <section className="space-y-5 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6">
        {analysis ? <><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">Result</p><h2 className="mt-2 text-xl font-semibold capitalize">{analysis.classification.replaceAll('_', ' ')}</h2><p className="mt-1 text-sm text-slate-500">Confidence {percent(analysis.confidence)} · percentages are model estimates, not certainty.</p></div><button type="button" onClick={() => setAnalysisSidebarOpen(value => !value)} className="inline-flex min-h-10 items-center gap-2 rounded-xl border border-slate-200 bg-white px-3 text-xs font-semibold text-slate-700 hover:bg-slate-50">{analysisSidebarOpen ? 'Sidebar: On' : 'Sidebar: Off'}</button></div>{persistedAnalysisPreview && <div className="overflow-hidden rounded-2xl border border-slate-200 bg-slate-950"><img src={persistedAnalysisPreview} alt="Analyzed image" className="max-h-[520px] w-full object-contain"/></div>}{storageComparison && <div className="grid gap-3 sm:grid-cols-3"><Metric label="Original" value={storageComparison.original}/><Metric label="Compressed preview" value={storageComparison.preview ?? 'Not created'}/><Metric label="Preview size" value={storageComparison.ratio ?? '—'}/></div>}<div className={`grid gap-3 ${analysisSidebarOpen ? 'lg:grid-cols-[1fr_280px]' : ''}`}><div className="space-y-4"><div><h3 className="text-sm font-semibold text-slate-900">Authenticity percentages</h3><p className="mt-1 text-xs leading-5 text-slate-500">These four values describe the returned likelihood breakdown.</p></div><div className="grid gap-3 sm:grid-cols-2"><ProbabilityCard label="AI-generated" value={analysis.ai_generated_probability}/><ProbabilityCard label="Edited / composited" value={analysis.edited_probability}/><ProbabilityCard label="Likely real" value={analysis.real_probability}/><ProbabilityCard label="Inconclusive" value={analysis.inconclusive_probability}/></div><div className="grid gap-3 sm:grid-cols-2"><Metric label="Confidence" value={percent(analysis.confidence)}/><Metric label="Conclusion" value={analysis.classification.replaceAll('_', ' ')}/></div><ResultList title="Evidence" items={analysis.evidence}/><ResultList title="Possible editing tools" items={analysis.possible_editing_tools} empty="None identified from the returned evidence."/><ResultList title="Limitations" items={analysis.limitations}/></div>{analysisSidebarOpen && <aside className="rounded-2xl border border-slate-200 bg-slate-50 p-4 lg:sticky lg:top-4 lg:self-start"><p className="text-[11px] font-semibold uppercase tracking-[.16em] text-slate-400">Analysis sidebar</p><h3 className="mt-1 text-base font-semibold text-slate-950">What the percentages mean</h3><div className="mt-4 space-y-3"><SideProbability label="AI-generated" value={analysis.ai_generated_probability}/><SideProbability label="Edited / composited" value={analysis.edited_probability}/><SideProbability label="Likely real" value={analysis.real_probability}/><SideProbability label="Inconclusive" value={analysis.inconclusive_probability}/></div><div className="mt-4 rounded-xl border border-slate-200 bg-white p-3 text-xs leading-5 text-slate-600"><strong className="text-slate-950">Confidence:</strong> {percent(analysis.confidence)} — how strongly the model supports its conclusion.</div></aside>}</div></> : <div className="grid min-h-[420px] place-items-center rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-10 text-center"><div><FileSearch className="mx-auto size-10 text-slate-400"/><h3 className="mt-4 text-base font-semibold text-slate-800">Analysis result will appear here</h3><p className="mt-2 max-w-md text-sm leading-6 text-slate-500">The uploaded image stays visible while analysis runs. After success, the original and a compressed WebP preview are saved with History.</p></div></div>}
      </section>
    </div>}
    {tab === 'video' && <div className="grid gap-6 lg:grid-cols-[1.05fr_.95fr]"><section className="space-y-5 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div><h2 className="text-lg font-semibold">Generate a silent video</h2><p className="mt-1 text-sm text-slate-500">Production video generation uses the configured Fal model. Audio generation is disabled.</p></div><label className="block text-sm font-medium">Prompt<textarea value={prompt} onChange={event => setPrompt(event.target.value)} rows={7} required minLength={3} placeholder="Describe product, scene, camera movement, pacing, and constraints…" className="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3 text-sm"/></label><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Duration<select value={duration} onChange={event => setDuration(Number(event.target.value))} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3"><option value={5}>5 seconds</option><option value={10}>10 seconds</option></select></label><div className="rounded-xl border border-slate-200 bg-slate-50 px-3 py-3"><p className="text-xs font-semibold text-slate-700">Quality</p><p className="mt-1 text-xs text-slate-500">Standard · 720p · 10 credits</p></div></div><label className="block text-sm font-medium">Aspect ratio<select value={aspectRatio} onChange={event => setAspectRatio(event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3"><option value="16:9">16:9</option><option value="9:16">9:16</option><option value="1:1">1:1</option></select></label><button type="button" disabled={loading} onClick={() => void submitVideo()} className="inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-xl bg-slate-950 px-4 text-sm font-semibold text-white disabled:opacity-50">{loading ? <Loader2 className="size-4 animate-spin"/> : <Video className="size-4"/>}Generate video</button>{message && <p className="text-sm font-medium text-slate-600">{message}</p>}</section><ResultPanel job={job} output={output} previewOutput={previewOutput} message={message} loadingHistory={loadingHistory} onDownload={downloadCurrent} onDelete={deleteCurrent} downloading={downloading} isVideo={isVideo}/></div>}
  </div>;
}

function Metric({ label, value }: { label: string; value: string }) { return <div className="rounded-2xl border border-slate-200 bg-slate-50 p-3"><p className="text-[11px] font-semibold uppercase tracking-wider text-slate-400">{label}</p><p className="mt-1 text-sm font-semibold text-slate-900">{value}</p></div>; }
function ProbabilityCard({ label, value }: { label: string; value: number }) { const pct = Math.max(0, Math.min(1, value)); return <div className="rounded-2xl border border-slate-200 bg-white p-4"><div className="flex items-center justify-between gap-3"><p className="text-sm font-semibold text-slate-900">{label}</p><p className="text-lg font-bold text-slate-950">{percent(pct)}</p></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-slate-950 transition-all" style={{ width: `${pct * 100}%` }}/></div></div>; }
function SideProbability({ label, value }: { label: string; value: number }) { const pct = Math.max(0, Math.min(1, value)); return <div><div className="flex items-center justify-between gap-3 text-xs"><span className="font-medium text-slate-700">{label}</span><span className="font-bold text-slate-950">{percent(pct)}</span></div><div className="mt-1.5 h-1.5 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-slate-900" style={{ width: `${pct * 100}%` }}/></div></div>; }
function ResultList({ title, items, empty = 'No returned items.' }: { title: string; items: string[]; empty?: string }) { return <div><h3 className="text-sm font-semibold text-slate-900">{title}</h3><div className="mt-2 space-y-2">{items.length ? items.map((item, index) => <div key={`${item}-${index}`} className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm leading-6 text-slate-600">{item}</div>) : <p className="text-sm text-slate-400">{empty}</p>}</div></div>; }
function ResultPanel(props: { job: Job | null; output: Output | null; previewOutput: Output | null; message: string; loadingHistory: boolean; onDownload: () => Promise<void>; onDelete: () => Promise<void>; downloading: boolean; isVideo: boolean }) {
  const { job, output, previewOutput, message, loadingHistory, onDownload, onDelete, downloading, isVideo } = props; const active = job?.status === 'queued' || job?.status === 'processing';
  return <section className="space-y-5 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6">{loadingHistory && <div className="flex items-center gap-2 text-sm text-slate-500"><Loader2 className="size-4 animate-spin"/>Opening saved history…</div>}{!job && !loadingHistory && <div className="grid min-h-[420px] place-items-center rounded-2xl border border-dashed border-slate-300 bg-slate-50 text-center"><div><Clock3 className="mx-auto size-9 text-slate-400"/><p className="mt-3 text-sm font-semibold text-slate-700">Your result will appear here</p><p className="mt-1 text-xs text-slate-500">Saved masters and previews are available through History.</p></div></div>}{job && <><div className="flex items-center justify-between gap-3"><span className={`inline-flex items-center gap-2 rounded-full px-3 py-1.5 text-xs font-semibold ${statusTone(job.status)}`}>{job.status === 'succeeded' ? <CheckCircle2 className="size-3.5"/> : active ? <Loader2 className="size-3.5 animate-spin"/> : <XCircle className="size-3.5"/>}{job.status}</span><span className="text-xs text-slate-400">{job.provider && job.model ? `${job.provider} · ${job.model}` : 'Provider-managed'}</span></div>{output?.url && <div className="overflow-hidden rounded-2xl border border-slate-200 bg-slate-950">{isVideo ? <video src={output.url} controls muted playsInline className="max-h-[520px] w-full object-contain"/> : <img src={(previewOutput?.url ?? output.url)} alt="Saved generated output" className="max-h-[520px] w-full object-contain"/>}</div>}{job.operation !== 'analyzeImage' && <div className="grid grid-cols-2 gap-3"><Metric label="Master" value={output ? bytesLabel(output.byte_size) : '—'}/><Metric label="Preview" value={previewOutput ? bytesLabel(previewOutput.byte_size) : isVideo ? 'Video master' : '—'}/></div>}{job.operation === 'analyzeImage' && analysisInfo(job.request?.analysisResult) && <div className="rounded-2xl border border-slate-200 bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">Authenticity summary</p><p className="mt-1 text-sm font-semibold text-slate-900">{analysisInfo(job.request?.analysisResult)?.classification.replaceAll('_', ' ')}</p><div className="mt-3 grid grid-cols-2 gap-2"><Metric label="AI-generated" value={percent(analysisInfo(job.request?.analysisResult)?.ai_generated_probability ?? 0)}/><Metric label="Edited" value={percent(analysisInfo(job.request?.analysisResult)?.edited_probability ?? 0)}/><Metric label="Real" value={percent(analysisInfo(job.request?.analysisResult)?.real_probability ?? 0)}/><Metric label="Inconclusive" value={percent(analysisInfo(job.request?.analysisResult)?.inconclusive_probability ?? 0)}/></div></div>}<p className="text-sm leading-6 text-slate-600">{message || (active ? 'Processing…' : job.status === 'succeeded' ? 'Saved successfully to private storage and history.' : job.error_message || 'The operation did not complete.')}</p>{job.status === 'succeeded' && <div className="flex flex-wrap gap-2"><button type="button" disabled={downloading || !output} onClick={() => void onDownload()} className="inline-flex min-h-10 items-center gap-2 rounded-xl border border-slate-200 px-3 text-xs font-semibold text-slate-700 disabled:opacity-50"><Download className="size-4"/>{downloading ? 'Preparing…' : 'Download master'}</button><button type="button" onClick={() => void onDelete()} className="inline-flex min-h-10 items-center gap-2 rounded-xl border border-red-200 px-3 text-xs font-semibold text-red-600"><Trash2 className="size-4"/>Delete</button></div>}</>}</section>;
}
function analysisInfo(value: unknown): AnalysisResult | null { return asAnalysis(value); }
