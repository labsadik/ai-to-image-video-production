import { FolderKanban, Plus, ArrowUpRight } from 'lucide-react';
import Link from 'next/link';
import { getSupabaseServerClient } from '@/server/supabase';
import { getSupabaseAdmin } from '@/server/supabase-admin';

export default async function ProjectsPage(){const client=await getSupabaseServerClient();const {data:{user}}=await client.auth.getUser();if(!user)return null;const {data:projects}=await getSupabaseAdmin().from('projects').select('id,name,platform,width,height,created_at,updated_at').eq('user_id',user.id).order('updated_at',{ascending:false});return <div className="space-y-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-slate-400">Projects</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Your creative workspaces</h1><p className="mt-2 text-sm text-slate-500">Projects keep canvases, generation history, and assets organized.</p></div><Link href="/dashboard/create" className="inline-flex items-center justify-center gap-2 rounded-xl bg-slate-950 px-4 py-3 text-sm font-semibold text-white"><Plus className="size-4"/> New project</Link></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{projects?.map(p=><Link href={`/dashboard/create?project=${p.id}`} key={p.id} className="group rounded-3xl border border-slate-200 bg-white p-4 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><div className="aspect-[16/10] rounded-2xl bg-gradient-to-br from-slate-100 via-white to-slate-200 p-5"><div className="flex h-full items-center justify-center rounded-xl border border-dashed border-slate-300"><FolderKanban className="size-7 text-slate-300"/></div></div><div className="mt-4 flex items-start justify-between gap-4"><div><p className="text-sm font-semibold">{p.name}</p><p className="mt-1 text-xs text-slate-400">{p.platform} · {p.width} × {p.height}</p></div><ArrowUpRight className="size-4 text-slate-300 group-hover:text-slate-700"/></div></Link>)}{!projects?.length&&<div className="sm:col-span-2 lg:col-span-3 rounded-3xl border border-dashed border-slate-300 bg-white p-12 text-center"><FolderKanban className="mx-auto size-8 text-slate-300"/><p className="mt-4 text-sm font-semibold">No projects yet</p><p className="mt-1 text-sm text-slate-500">Start your first visual and your workspace will appear here.</p><Link href="/dashboard/create" className="mt-5 inline-flex rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">Create visual</Link></div>}</div></div>}
