'use client';

import Link from 'next/link';
import { FormEvent, useState } from 'react';
import { ArrowRight, Check, LockKeyhole, Sparkles } from 'lucide-react';
import { getSupabaseBrowserClient } from '@/lib/supabase/client';
import { Button } from '@/components/ui/button';

export default function LoginPage() {
  const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [acceptedTerms,setAcceptedTerms]=useState(false); const [message,setMessage]=useState(''); const [loading,setLoading]=useState(false);
  async function submit(event:FormEvent){event.preventDefault();if(!acceptedTerms){setMessage('Please accept the Terms & Responsible Use before signing in.');return;}setLoading(true);setMessage('');const {data,error}=await getSupabaseBrowserClient().auth.signInWithPassword({email,password});if(!error&&data.user){await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({activity:true})});window.location.href='/dashboard';return;}setMessage(error?.message??'Unable to sign in.');setLoading(false);}
  return <main className="grid min-h-screen bg-slate-950 lg:grid-cols-2"><section className="hidden bg-gradient-to-br from-slate-950 via-slate-900 to-slate-800 p-8 text-white lg:flex lg:flex-col lg:justify-between lg:p-12"><div><Link href="/" className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-2xl bg-white text-slate-950"><Sparkles className="size-5"/></span><span><span className="block text-sm font-semibold">Solamentis</span><span className="block text-xs text-slate-400">Creative Intelligence</span></span></Link><h1 className="mt-24 max-w-lg text-5xl font-semibold tracking-tight">Your creative system, ready when you are.</h1><p className="mt-6 max-w-lg leading-7 text-slate-400">A professional workspace for AI image generation, editing, exports, and project history.</p></div><p className="text-xs text-slate-500">Private by default · Provider-independent · Configurable</p></section><section className="flex min-h-screen items-center justify-center bg-slate-50 px-4 py-8 sm:px-6 sm:py-12"><form onSubmit={submit} className="w-full max-w-md rounded-[28px] border border-slate-200 bg-white p-6 shadow-2xl shadow-slate-950/10 sm:p-8"><div className="mb-8 lg:hidden"><Link href="/" className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-2xl bg-slate-950 text-white"><Sparkles className="size-5"/></span><span className="text-sm font-semibold">Solamentis</span></Link></div><div className="grid size-11 place-items-center rounded-2xl bg-slate-100"><LockKeyhole className="size-5"/></div><h2 className="mt-5 text-3xl font-semibold tracking-tight">Welcome back</h2><p className="mt-2 text-sm text-slate-500">Sign in to continue to your workspace.</p><div className="mt-8 space-y-4"><label className="block text-sm font-medium">Email<input className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none transition focus:border-slate-400 focus:ring-4 focus:ring-slate-100" type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></label><label className="block text-sm font-medium">Password<input className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none transition focus:border-slate-400 focus:ring-4 focus:ring-slate-100" type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password"/></label></div><label className="mt-5 flex cursor-pointer items-start gap-3 rounded-2xl border border-slate-200 bg-slate-50 p-3.5 text-xs leading-5 text-slate-600"><input type="checkbox" required checked={acceptedTerms} onChange={e=>setAcceptedTerms(e.target.checked)} className="mt-1 size-4 shrink-0 accent-slate-950"/><span>I agree to the <Link href="/terms" className="font-semibold text-slate-950 underline underline-offset-2">Terms & Responsible Use</Link>. I understand that content I generate or upload must be used lawfully and that I am responsible for reviewing and using my output.</span></label>{message&&<p role="alert" className="mt-4 rounded-xl bg-red-50 px-3 py-2 text-sm text-red-700">{message}</p>}<Button type="submit" loading={loading} className="mt-6 w-full">{loading?'Signing in…':'Sign in'}{!loading&&<ArrowRight className="size-4"/>}</Button><p className="mt-6 flex items-center justify-center gap-1 text-sm text-slate-500"><Check className="size-4 text-emerald-600"/> Protected session · <Link href="/signup" className="font-semibold text-slate-900">Create an account</Link></p></form></section></main>;
}
