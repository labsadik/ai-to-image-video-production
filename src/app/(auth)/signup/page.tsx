'use client';

import Link from 'next/link';
import { FormEvent, useState } from 'react';
import { ArrowRight, Globe2, Sparkles, UserRound } from 'lucide-react';
import { getSupabaseBrowserClient } from '@/lib/supabase/client';
import { Button } from '@/components/ui/button';

const countries = [['IN','India'],['US','United States'],['BD','Bangladesh'],['GB','United Kingdom'],['AE','United Arab Emirates']];

export default function SignupPage() {
  const [name,setName]=useState(''); const [phone,setPhone]=useState(''); const [country,setCountry]=useState('IN'); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [acceptedTerms,setAcceptedTerms]=useState(false); const [message,setMessage]=useState(''); const [loading,setLoading]=useState(false);
  async function submit(event: FormEvent) { event.preventDefault(); if(!acceptedTerms){setMessage('Please accept the Terms & Responsible Use before creating your account.');return;} setLoading(true); setMessage(''); const client=getSupabaseBrowserClient(); const { data,error }=await client.auth.signUp({email,password,options:{data:{full_name:name,phone,country_code:country}}}); if(error){setMessage(error.message);setLoading(false);return;} if(data.user) await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({full_name:name,phone,country_code:country})}); setMessage(data.session?'Account created. Opening your workspace…':'Account created. Check your email to verify your account.'); if(data.session) window.location.href='/dashboard'; else setLoading(false); }
  return <main className="min-h-screen bg-slate-50 px-4 py-8 sm:px-6 sm:py-12"><div className="mx-auto max-w-6xl overflow-hidden rounded-[30px] border border-slate-200 bg-white shadow-2xl shadow-slate-950/10 lg:grid lg:grid-cols-[.8fr_1.2fr]"><section className="bg-slate-950 p-6 text-white sm:p-8 lg:p-12"><Link href="/" className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-2xl bg-white text-slate-950"><Sparkles className="size-5" /></span><span><span className="block text-sm font-semibold">Solamentis</span><span className="block text-xs text-slate-400">Creative Intelligence</span></span></Link><div className="mt-14 lg:mt-20"><p className="text-xs font-semibold uppercase tracking-[.2em] text-slate-400">Start your studio</p><h1 className="mt-4 text-4xl font-semibold tracking-tight">One workspace for every visual.</h1><p className="mt-5 leading-7 text-slate-400">Your plan, credits, profile, country pricing, projects, assets, and generation history stay connected through Supabase.</p></div><div className="mt-8 grid gap-3 text-sm text-slate-300 sm:mt-10 sm:grid-cols-3 lg:grid-cols-1"><div className="rounded-2xl border border-white/10 bg-white/5 p-4">10 free credits every month</div><div className="rounded-2xl border border-white/10 bg-white/5 p-4">Private project storage</div><div className="rounded-2xl border border-white/10 bg-white/5 p-4">Platform-ready exports</div></div></section><section className="p-6 sm:p-8 lg:p-12"><div className="mx-auto max-w-xl"><div className="flex size-11 items-center justify-center rounded-2xl bg-slate-100"><UserRound className="size-5" /></div><h2 className="mt-5 text-3xl font-semibold tracking-tight">Create your account</h2><p className="mt-2 text-sm text-slate-500">Use accurate profile details so the workspace and regional pricing can stay consistent.</p><form onSubmit={submit} className="mt-8 space-y-4"><label className="block text-sm font-medium">Full name<input required autoComplete="name" value={name} onChange={e=>setName(e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100" placeholder="Your name" /></label><div className="grid gap-4 sm:grid-cols-2"><label className="block text-sm font-medium">Phone number<input autoComplete="tel" value={phone} onChange={e=>setPhone(e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100" placeholder="+91 …" /></label><label className="block text-sm font-medium">Country<select value={country} onChange={e=>setCountry(e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100">{countries.map(([code,label])=><option key={code} value={code}>{label}</option>)}</select></label></div><label className="block text-sm font-medium">Email<input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100" placeholder="you@company.com" /></label><label className="block text-sm font-medium">Password<input required minLength={8} type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100" placeholder="At least 8 characters" /></label><label className="flex cursor-pointer items-start gap-3 rounded-2xl border border-slate-200 bg-slate-50 p-4 text-xs leading-5 text-slate-600"><input type="checkbox" required checked={acceptedTerms} onChange={e=>setAcceptedTerms(e.target.checked)} className="mt-1 size-4 shrink-0 accent-slate-950"/><span>I agree to the <Link href="/terms" className="font-semibold text-slate-950 underline underline-offset-2">Terms & Responsible Use</Link>. I understand that content I generate or upload must be used lawfully and that I am responsible for reviewing and using my output.</span></label>{message && <p role="alert" className="rounded-xl bg-red-50 px-3 py-2 text-sm text-red-700">{message}</p>}<Button type="submit" loading={loading} className="w-full">{loading?'Creating workspace…':'Create workspace'} {!loading&&<ArrowRight className="size-4"/>}</Button></form><div className="mt-6 flex items-start gap-2 rounded-2xl border border-slate-200 bg-slate-50 p-4 text-xs leading-5 text-slate-500"><Globe2 className="mt-0.5 size-4 shrink-0" /> Country is used as a pricing/display signal. Billing provider rules remain authoritative for actual billing.</div><p className="mt-6 text-center text-sm text-slate-500">Already have an account? <Link href="/login" className="font-semibold text-slate-900">Sign in</Link></p></div></section></div></main>;
}
