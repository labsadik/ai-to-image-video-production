'use client';

import Link from 'next/link';
import { FormEvent, useState } from 'react';
import { ArrowRight, Globe2, Sparkles, UserRound } from 'lucide-react';
import { getSupabaseBrowserClient } from '@/lib/supabase/client';

const countries = [['IN','India'],['US','United States'],['BD','Bangladesh'],['GB','United Kingdom'],['AE','United Arab Emirates']];

export default function SignupPage() {
  const [name,setName]=useState(''); const [phone,setPhone]=useState(''); const [country,setCountry]=useState('IN'); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [message,setMessage]=useState(''); const [loading,setLoading]=useState(false);
  async function submit(event: FormEvent) {
    event.preventDefault(); setLoading(true); setMessage('');
    const client=getSupabaseBrowserClient();
    const { data,error }=await client.auth.signUp({email,password,options:{data:{full_name:name,phone,country_code:country}}});
    if(error){setMessage(error.message);setLoading(false);return;}
    if(data.user){ await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({full_name:name,phone,country_code:country})}); }
    setMessage(data.session?'Account created. Opening your workspace…':'Account created. Check your email to verify your account.');
    if(data.session) window.location.href='/dashboard'; else setLoading(false);
  }
  return <main className="min-h-screen bg-slate-50 px-6 py-12"><div className="mx-auto max-w-6xl overflow-hidden rounded-[32px] border border-slate-200 bg-white shadow-2xl shadow-slate-950/10 lg:grid lg:grid-cols-[.8fr_1.2fr]"><section className="bg-slate-950 p-8 text-white lg:p-12"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-2xl bg-white text-slate-950"><Sparkles className="size-5" /></span><span><span className="block text-sm font-semibold">Solamentis</span><span className="block text-xs text-slate-400">Creative Intelligence</span></span></div><div className="mt-20"><p className="text-xs font-semibold uppercase tracking-[.2em] text-slate-400">Start your studio</p><h1 className="mt-4 text-4xl font-semibold tracking-tight">One workspace for every visual.</h1><p className="mt-5 leading-7 text-slate-400">Your plan, credits, profile, country pricing, projects, assets, and generation history stay connected through Supabase.</p></div><div className="mt-10 grid gap-3 text-sm text-slate-300"><div className="rounded-2xl border border-white/10 bg-white/5 p-4">10 free credits every month</div><div className="rounded-2xl border border-white/10 bg-white/5 p-4">Private project storage</div><div className="rounded-2xl border border-white/10 bg-white/5 p-4">Platform-ready exports</div></div></section><section className="p-8 lg:p-12"><div className="mx-auto max-w-xl"><div className="flex size-11 items-center justify-center rounded-2xl bg-slate-100"><UserRound className="size-5" /></div><h2 className="mt-5 text-3xl font-semibold tracking-tight">Create your account</h2><p className="mt-2 text-sm text-slate-500">We use your profile details to personalize the workspace and regional pricing.</p><form onSubmit={submit} className="mt-8 space-y-4"><label className="block text-sm font-medium">Full name<input required value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100" placeholder="Your name" /></label><div className="grid gap-4 sm:grid-cols-2"><label className="block text-sm font-medium">Phone number<input value={phone} onChange={e=>setPhone(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100" placeholder="+91 …" /></label><label className="block text-sm font-medium">Country<select value={country} onChange={e=>setCountry(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100">{countries.map(([code,label])=><option key={code} value={code}>{label}</option>)}</select></label></div><label className="block text-sm font-medium">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100" placeholder="you@company.com" /></label><label className="block text-sm font-medium">Password<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-3.5 py-3 outline-none focus:border-slate-400 focus:ring-4 focus:ring-slate-100" placeholder="At least 8 characters" /></label>{message && <p className="rounded-xl bg-slate-100 px-3 py-2 text-sm text-slate-600">{message}</p>}<button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-slate-950 px-4 py-3.5 text-sm font-semibold text-white disabled:opacity-50">{loading?'Creating workspace…':'Create workspace'} {!loading&&<ArrowRight className="size-4"/>}</button></form><div className="mt-6 flex items-start gap-2 rounded-2xl border border-slate-200 bg-slate-50 p-4 text-xs leading-5 text-slate-500"><Globe2 className="mt-0.5 size-4 shrink-0" /> Country is used as a pricing/display signal. Billing provider rules remain authoritative for actual billing.</div><p className="mt-6 text-center text-sm text-slate-500">Already have an account? <Link href="/login" className="font-semibold text-slate-900">Sign in</Link></p></div></section></div></main>;
}
